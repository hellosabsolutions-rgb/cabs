# Dependencies
node_modules
dist
dist-ssr

# Logs
*.log
npm-debug.log*

# Secrets — never commit production secrets
.env
.env.local
.env.production.local
.env.*.local

# .env.production is committed (public VITE_* URLs only)

# Firebase / Google Cloud service account JSON (never commit)
*firebase-adminsdk*.json
opsiva-*.json

# Editor / OS
.DS_Store
.vscode/*
!.vscode/extensions.json
.idea
